services:
 postgres:
  image: postgres:16
  environment:
   POSTGRES_USER: weather
   POSTGRES_PASSWORD: weather
   POSTGRES_DB: weather_dw
  ports:
   - "5432:5432"
  volumes:
   - pgdata:/var/lib/postgresql/data


volumes:
 pgdata: